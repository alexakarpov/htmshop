server {
  listen 80;
  listen [::]:80;
  server_name transylvania.bostonmonks.com;
  location / {
    proxy_pass http://unix:/run/gunicorn.sock;
  }
  location /static {
    autoindex on;
    alias /var/static_root/;
  }
}
